# Demo 验证记录

日期：2026-09-23。环境：本机 Chrome headless、独立临时浏览器上下文。

已通过：
- JavaScript 语法检查。
- 四页阅读、翻页、调整预览、采用、撤销。
- 分享 PNG 下载。
- 图片上传、旋转处理、本地保存、刷新找回。
- 下载原图与上传文件逐字节一致。
- 自传画作的真实 AI 功能保持关闭。
- 演示任务取消及提交成功后的刷新恢复。
- 删除成功后的刷新持久化。
- 360 / 390 / 430 像素宽度无横向溢出；桌面和手机截图人工检查。修复了手机标题换行和插画遮挡说明问题。
- 四页打印导出，实际生成 PDF 并解析确认共 4 页。
- 流程测试无页面脚本异常。

未验证：实体 iOS / Android / 微信浏览器、麦克风录音硬件及权限矩阵、真实模型效果、服务端账户隔离、实际支付与退款。上述未接入服务不宣称通过 PRD 正式验收。

截图、分享文件、PDF：test-results/（测试生成目录，不进入版本管理）。

## Poixe 网页接入 · 2026-09-24

新增模拟供应商测试已通过：
- 一次文字＋三次带原画参考的生图，完整四页结果。
- 重复提交不重复请求模型；凭据不匹配无法读取任务。
- 进程重建后找回已生成页面，不重新请求该页。
- 生成期间删除，不恢复已删除内容。
- 网页上传、AI 单独授权、服务端任务、阅读及刷新位置恢复。
- 真实故事正确展示 AI/未人工审核标识，不出现模拟调整入口。
- `.env`、任务目录、后端模块、测试脚本不能通过静态服务读取。

以上新增测试使用模拟模型响应，无真实费用。此前真实 Poixe 单图与找回已成功；新增完整原画参考三页链路及 dc8 线路仍待用户配置环境后联调。当前运行服务未读取到密钥，用户可在已配置环境的终端执行 `npm run ai:configure` 后刷新网页。
